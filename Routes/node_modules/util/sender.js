const { getErrorMessage } = require('./message');

module.exports = {
  makeResponse: function(data) {
    let errorCode;
    let result;

    if (!data) {
      errorCode = 1;

      result = {
        error: true,
        code: 1,
        errorMessage: getErrorMessage(1)
      }
    }

    if (data) {
      errorCode = 1;

      result = {
        error: true,
        code: 1,
        errorMessage: getErrorMessage(1)
      }
    }

    return result;
  },

  send: function() {

  },
};
